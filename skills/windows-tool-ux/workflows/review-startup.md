# 冷啟動瓶頸定位與假死消除流程 (Review Startup Workflow)

1. **進入點程式碼審查**：
   - 檢查 `App.xaml.cs`、`Main()` 或 `main.py` 在 `window.Show()` 之前的所有呼叫。
2. **耗時作業移轉**：
   - 識別所有非必要之本機檔案比對、設定載入或日誌滾動，重構為背景 Task。
3. **骨架屏／載入指示器**：
   - 若特定工具確實需要載入 100MB+ 模型權重，主視窗必須立即呈現，並顯示載入中動畫與文字說明，嚴禁讓 Exe 處於無視窗假死狀態。
